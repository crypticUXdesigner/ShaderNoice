---
description: Runtime and compilation must not mutate the graph; use store/callbacks and data-model types.
globs: "src/runtime/**/*.ts,src/shaders/compilation/**/*.ts,src/shaders/NodeShaderCompiler.ts"
alwaysApply: false
---

# Runtime and compilation

Read-only graph from store/callbacks; **never** assign into `graph`, `graph.nodes`, or `graph.connections`. Use **`src/data-model`** types.

Parameter updates flow like the app (`onParameterChange`, etc.); compiler receives graphs that already passed **`validateGraph`** (see serialization rule).

After edits: grep/scan for mutations in `src/runtime/**` and `src/shaders/**` (compilation); run `npm run build` + relevant tests.

**Refs:** `.cursor/rules/data-model/graph-updates.mdc`, `docs/architecture/graph-and-platform-boundaries.md` (graph ownership, compilation / runtime seams), `docs/architecture/preview-and-recompilation.md`, `docs/architecture/compilation-worker.md`.
