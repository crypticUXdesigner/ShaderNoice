# Review performance → optional WPs

Bench / reason about hotspots; propose scoped optimizations — **`review-performance-skill`** mirrors flow.

Honor **`project-workflow.mdc`**, **`workpkg-hygiene.mdc`**.

---

1. Measure or inspect hotspots (GPU/CPU hotspots, stray allocations, pathological rerenders — cite evidence when possible).
2. Share ranked issues (**impact**, **frequency**, effort **S|M|L**, location, summary).
3. Wait for prioritization signal.
4. After approval: carve tasks (**`define-tasks`**) without bloating unrelated `_OVERVIEW` sections.
