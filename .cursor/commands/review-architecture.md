# Review architecture → optional WPs

Surface structural risks / refactors-as-WPs — **skills:** **`review-architecture-skill`** for guided flow.

Honor **`project-workflow.mdc`** + **`workpkg-hygiene.mdc`**.

---

1. Inspect coupling, boundaries, debt hotspots (prioritize graph ↔ runtime ↔ UI seams).
2. Emit table/list: impact, effort **S|M|L**, file/area hint, gist, whether WP-worthy.
3. **Stop** until user selects targets.
4. Only after thumbs-up: author tasks via **`define-tasks`** (or extend an existing **`docs/implementation/<slug>/`** package) syncing `_OVERVIEW` structure expectations.
